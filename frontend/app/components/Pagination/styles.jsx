import styled from 'styled-components'
import { Button } from '..'

export const StyledButtonPage = styled(Button)`
  outline: none;
  padding: 10px;
`

export const StyledButtonArrows = styled(StyledButtonPage)`
  padding: 0 3px;
`
